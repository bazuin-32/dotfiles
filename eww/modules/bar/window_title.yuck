(deflisten _window_title :initial "init"
  ``)

(defwidget window_title []
  (box :class "window_title"
       :orientation "h"
       :space-evenly true
       :halign "center"
       :spacing 0
    (label :text _window_title)))
