(defpoll time :interval "1s"
              :initial "initial"
              :run-while true
  `date '+%I:%M %p'`)


(defwidget time []
  (box :class "time"
       :orientation "h"
       :space-evenly true
       :halign "center"
       :spacing 0
    (label :text time)))
