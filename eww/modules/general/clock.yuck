(defpoll time :interval "1s"
              :initial "initial"
              :run-while true
  `date '+%I:%M %p'`)


(defwidget clock []
  (box :class "time"
       :orientation "h"
       :space-evenly true
       :halign "end"
       :spacing 0
    (label :text time)))
